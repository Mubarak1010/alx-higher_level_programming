#!/usr/bin/python3
"""Module."""


def is_same_class(obj, a_class):
    """Class declaration.

    Args:
        obj (obj): object
        a_class (class): class instance"""

    if type(obj) == a_class:
        return (True)

    return (False)
