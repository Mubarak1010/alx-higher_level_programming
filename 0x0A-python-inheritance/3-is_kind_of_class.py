#!/usr/bin/python3
""" Module. """


def is_kind_of_class(obj, a_class):
    """ Class declaration. 

    Args:
        obj (obj): object
        a_class (tyoe): class type
        """

    if isinstance(obj, a_class):
        return (True)

    return (False)
